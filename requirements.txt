# BeekKon Bridge - Dependencies
PyNaCl>=1.5.0          # Ed25519 + Curve25519 + AES-256-GCM
msgpack>=1.0.5         # Binary serialization
pytest>=7.4.0          # Unit tests
pytest-asyncio>=0.21.0 # Async tests
# zeroconf REMOVED - replaced by custom UDP broadcast discovery